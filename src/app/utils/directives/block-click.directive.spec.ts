import { BlockClickDirective } from './block-click.directive';

describe('BlockClickDirective', () => {
  it('should create an instance', () => {
    const directive = new BlockClickDirective();
    expect(directive).toBeTruthy();
  });
});
